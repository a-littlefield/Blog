
<%= form_with model: @post, local: true, class: 'post-form' do |f| %>
	<%= f.text_field :title, placeholder: 'Post Title', class: 'post-title' %>
	<%= f.text_area :content, size: "30x5", placeholder: 'Post content' %>
	<%= f.submit %>	
<% end %>	